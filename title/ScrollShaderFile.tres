[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;

uniform float speed = 1;
uniform float offset = 0;

void fragment() {
	COLOR = texture(TEXTURE, UV + vec2(offset+speed*0.02*TIME));
}"
